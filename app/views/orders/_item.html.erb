
  <tr>
    <td>
      <%= image_tag item.product.image.tiny, class: 'main-img' %>
    </td>
    <td>
      <h4><%= item.product.name %></h4>
      <br>
      <p><%= item.product.description %></p>
    </td>
    <td>
      <%= item.item_price %>
    </td>
    <td>
      <%= item.quantity %>
    </td>

    <td>
      <%= item.total_price %>
    </td>
  </tr>
